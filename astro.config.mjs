// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
// NOTE: update `site` if kondzio1289 isn't your GitHub username, or if this
// repo isn't named kondzio1289.github.io (in which case add a `base` too).
export default defineConfig({
  site: 'https://kondzio1289.github.io',
});
